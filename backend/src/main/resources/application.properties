# Application
spring.application.name=ai-proctoring-backend
frontend.url=http://localhost:3000
server.port=8080

# Database
spring.datasource.url=jdbc:postgresql://127.0.0.1:5432/proctoring_db
spring.datasource.username=postgres
spring.datasource.password=1234
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
# Let Hibernate auto-detect the dialect for better compatibility
# spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.datasource.hikari.connection-timeout=30000
spring.datasource.hikari.maximum-pool-size=10
spring.h2.console.enabled=true

# JWT
jwt.secret=mySecretKey_AI_Proctoring_2024_SuperSecure_256bits_long_key
jwt.expiration=86400000

# Mail
spring.mail.host=smtp.gmail.com
spring.mail.port=465
spring.mail.username=televisionairtel01@gmail.com
spring.mail.password=xcgjqdbdocwltkgc
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=false
spring.mail.properties.mail.smtp.ssl.enable=true
spring.mail.properties.mail.smtp.socketFactory.port=465
spring.mail.properties.mail.smtp.socketFactory.class=javax.net.ssl.SSLSocketFactory
spring.mail.properties.mail.smtp.socketFactory.fallback=false
spring.mail.properties.mail.debug=true

# File Upload
spring.servlet.multipart.max-file-size=100MB
spring.servlet.multipart.max-request-size=100MB
server.tomcat.max-swallow-size=100MB
server.tomcat.max-http-form-post-size=100MB

# CORS
cors.allowed-origins=http://localhost:3000

# Compiler Settings (100% Free Local Execution)
compiler.mode=local

# AI Monitor
ai.monitor.url=http://localhost:5000

# Logging
logging.level.com.proctoring=DEBUG
